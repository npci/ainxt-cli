[package]
license = "Apache-2.0"
name = "ainxt-version"
version = "1.0.0"
edition.workspace = true
description = "Lockstepped ainxt CLI version."

[dependencies]
semver = { workspace = true }

[lints]
workspace = true
