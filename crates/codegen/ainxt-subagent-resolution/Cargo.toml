[package]
publish.workspace = true
license = "Apache-2.0"
name = "ainxt-subagent-resolution"
version = "0.1.0"
edition.workspace = true
description = "Subagent configuration resolution: merges persona, role, and spawn-time overrides into a resolved spec"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
ainxt-sampling-types = { path = "../ainxt-sampling-types" }
ainxt-tools = { path = "../ainxt-tools" }
ainxt-tool-types.workspace = true
# TODO(phase2): add these when resolve_subagent_spec() composition is implemented:
# ainxt-agent = { path = "../ainxt-agent" }  # AgentDefinition lookup
# ainxt-fast-worktree = { path = "../ainxt-fast-worktree" }  # worktree creation

[dev-dependencies]
tempfile = { workspace = true }
toml = { workspace = true }

[lints]
workspace = true
