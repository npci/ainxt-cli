[package]
license = "Apache-2.0"
name = "ainxt-policy-fetch"
version = "0.1.0"
edition.workspace = true
description = "Fetches the signed gateway policy overlay. Kept separate so ainxt-policy stays offline."

[dependencies]
ainxt-http = { workspace = true }
ainxt-policy = { workspace = true }
ainxt-policy-types = { workspace = true }
reqwest = { workspace = true }
tokio = { workspace = true, features = ["rt", "time", "macros"] }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
