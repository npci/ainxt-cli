[package]
publish.workspace = true
license = "Apache-2.0"
name = "ainxt-intent"
version = "0.1.0"
edition.workspace = true
description = "Derives the capability set a concrete action requests. Describes, never decides."

[dependencies]
ainxt-shell-ast = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
pretty_assertions = { workspace = true }

[lints]
workspace = true
